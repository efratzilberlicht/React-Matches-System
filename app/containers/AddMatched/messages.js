/*
 * AddMatched Messages
 *
 * This contains all the text for the AddMatched container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddMatched';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddMatched container!',
  },
});
